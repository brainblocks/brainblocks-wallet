// @flow
import { css } from 'emotion'
import { addStyles } from 'destyle'

addStyles('TxForm', props => ({
  root: css``,
  textComponent: css`
    margin-left: 22px;
  `,
  defTableInField: css`
    flex-grow: 1;
    padding: 18px 22px;
  `
}))
